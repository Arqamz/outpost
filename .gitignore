# Generated VM artifacts (templates, overlays, seeds, downloaded images)
.var/

# Local notes/runbooks
notes/

# Live static-node manifest — holds a real instance IP that changes per launch.
# node.ec2.example.yaml is the committed template; node.ec2.yaml is per-machine.
node.ec2.yaml

# Built apptainer images
demo/*.sif

# Generated ansible inventory
infra/ansible/inventory/hosts.ini

# Nix
result
result-*

# Editor / OS cruft
*.swp
.DS_Store

# Python
__pycache__/
*.pyc

# Claude Code local session state
.claude/scheduled_tasks.lock
